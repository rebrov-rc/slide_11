var _a;
var obj = {
    num: 1,
    next: (_a = {
            num: 2,
            next: {
                num: 3,
                next: stop
            },
            let: let, arr: number[] = new Array,
            "function": rec(obj), number: number
        },
        _a[] = {
            "return": arr.push(obj.num) += rec(obj.next)
        },
        // console.log(rec(obj))
        // arr.push(obj.num)
        _a.console = console,
        _a. = .log(arr),
        _a.console = console,
        _a. = .log(obj),
        _a)
};
